## No Spec Changes

This change is a pure implementation bug fix in EXIF date parsing. No specification-level requirements are added, modified, or removed. The existing behavior for photo upload and EXIF extraction remains the same from a requirements perspective; only the timezone handling during parsing is corrected.
