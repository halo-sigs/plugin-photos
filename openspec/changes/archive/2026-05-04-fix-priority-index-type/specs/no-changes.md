## No Spec Changes

This change is an internal index type correction on `PhotoPlugin`. No specification-level requirement is added, modified, or removed:

- No spec under `openspec/specs/` describes the type of a database index key.
- The `group-priority-editor` spec governs console UI behavior (input fields, persistence) and is unaffected by how the index stores the value internally.
- No query, finder, or API contract changes.
